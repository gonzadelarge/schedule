<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/index.css">
    <script src="/js/index.js"></script>
    <title>{{title}}</title>
</head>

<body>
    <div class="b-container bg-2">
       <div class="fake-div"></div>
       <div id="menu" class="b-header">
           <div id="btn-menu" class="b-header__icon"></div>
            <div class="b-header__logo">
                <a href="/"><h2 class="b-header__title">App Schedule</h2></a>
            </div>
            <div class="b-header__links">
                <a class="b-header__link" href="/todos">Todos</a>
                <a class="b-header__link" href="/meetings">Meets</a>
                <a class="b-header__link" href="/user">User</a>
                <form class="b-header__form" action="/auth/logout" method="POST">
                    <button class="b-header__button" type="submit">Out</button>
                </form>
            </div>
        </div>
        <form class="b-controls" action="/schedule/add?_method=PUT" method="POST">
            <div class="b-form__inputBox b-controls__input"><input class="b-form__input" type="text" name="filter" placeholder="Filter"/></div>
              <div class="b-form__inputBox b-controls__button"><input class="b-form__input b-button" type="submit" value="Refresh"></div>
        </form>
        <div class="b-schedule">
            <h2 class="b-schedule__link">Tareas</h2>
            <div class="b-schedule__cards">
                {{#each scheduleContain.todo}}
                    <ul class="b-card">
                        <a href="todos/todo/{{this.id}}">
                            <div class="b-card__heading">
                                <li>{{this.name}}</li>
                                {{#if this.done}}<li>¿Terminado? Sí</li>{{/if}}
                                {{#if this.done}} {{else}}<li>¿Terminado? No</li>{{/if}}
                            </div>
                            <div class="b-card__block">
                                <li>{{this.date}}</li>
                                <li>{{this.message}}</li>
                            </div>
                        </a>
                    </ul>
                {{/each}} 
            </div>
        </div>
        <div class="b-schedule">
            <h2 class="b-schedule__link">Reuniones</h2>
            <div class="b-schedule__cards">
                {{#each scheduleContain.meeting}}
                    <ul class="b-card">
                        <a href="todos/todo/{{this.id}}">
                            <div class="b-card__heading">
                                <li>{{this.name}}</li>
                                {{#if this.done}}<li>¿Terminado? Sí</li>{{/if}}
                                {{#if this.done}} {{else}}<li>¿Terminado? No</li>{{/if}}
                            </div>
                            <div class="b-card__block">
                                <li>{{this.date}}</li>
                                <li>{{this.message}}</li>
                            </div>
                        </a>
                    </ul>
                {{/each}} 
            </div>
        </div>
        <div class="b-footer">
            <p class="b-footer__text">All rights reserved</p>
        </div>
    </div>
</body>
</html>